import 'package:flutter/material.dart';

class ClanFlagManagerScreen extends StatelessWidget {
  const ClanFlagManagerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Gerenciar Bandeira do Clã'),
      ),
      body: const Center(
        child: Text('Conteúdo do Gerenciador de Bandeiras do Clã'),
      ),
    );
  }
}


